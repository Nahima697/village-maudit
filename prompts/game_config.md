Jeu Roblox : "Village Maudit".
Type : Module script partagé (chemin : src/ReplicatedStorage/Modules/GameConfig.lua).

# Objectif
Créer une table de configuration globale pour centraliser les constantes du jeu.

# Contraintes Techniques Strictes
- Définir les durées (ex: DayDuration = 300 secondes, NightDuration = 300 secondes, VotingDuration = 60 secondes).
- Définir le ratio de traîtres (ex: 1 traître pour 5 joueurs).
- Le module doit retourner une table standard Luau via `return GameConfig`.
- Le code doit être propre et optimisé, sans balise markdown ni texte superflu.

# Format de Sortie
Renvoie UNIQUEMENT le code Luau en texte brut. N'inclus aucune explication, et n'utilise AUCUNE balise markdown.
